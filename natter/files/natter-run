#!/bin/sh

log_file="$1"
shift

mkdir -p "$(dirname "$log_file")"
if [ "${NATTER_RUNTIME:-go}" = "go" ]; then
	exec "${NATTER_GO_BIN:-/usr/bin/natter-go}" "$@" >> "$log_file" 2>&1
fi
exec "${NATTER_PY_BIN:-/usr/bin/Natter}" "$@" >> "$log_file" 2>&1
