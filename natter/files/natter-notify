#!/bin/sh

set -u

: "${NATTER_QBITTORRENT_SH:=/usr/share/natter/natter-qbittorrent.sh}"
: "${NATTER_COMMON_SH:=/usr/share/natter/natter-common.sh}"
: "${NATTER_LOGGER_BIN:=logger}"
: "${NATTER_CURL_BIN:=curl}"
: "${NATTER_UCI_BIN:=uci}"
: "${NATTER_FIREWALL_INIT:=/etc/init.d/firewall}"

. "$NATTER_QBITTORRENT_SH"
. "$NATTER_COMMON_SH"

protocol="${1:-}"
inner_ip="${2:-}"
inner_port="${3:-}"
outer_ip="${4:-}"
outer_port="${5:-}"

env_file="${NATTER_NOTIFY_ENV:-}"
if [ -n "$env_file" ] && [ -r "$env_file" ]; then
	# shellcheck disable=SC1090
	. "$env_file"
fi

log() {
	local message="$*"

	"$NATTER_LOGGER_BIN" -t natter-notify "$message" >/dev/null 2>&1 || true
	if [ -n "${NATTER_LOG_FILE:-}" ]; then
		mkdir -p "$(dirname "$NATTER_LOG_FILE")" >/dev/null 2>&1 || true
		printf '%s %s\n' "$(date '+%Y-%m-%d %H:%M:%S')" "$message" >> "$NATTER_LOG_FILE" 2>/dev/null || true
	fi
}

natter_one_line() {
	printf '%s' "${1:-}" | tr '\r\n' '  ' | sed 's/[[:space:]][[:space:]]*/ /g; s/^ //; s/ $//' | cut -c 1-300
}

natter_json_port() {
	printf '%s\n' "${1:-}" | sed -n 's/.*"port"[[:space:]]*:[[:space:]]*\([0-9][0-9]*\).*/\1/p' | head -n 1
}

natter_json_listen_port() {
	printf '%s\n' "${1:-}" | sed -n 's/.*"listen_port"[[:space:]]*:[[:space:]]*\([0-9][0-9]*\).*/\1/p' | head -n 1
}

natter_json_string() {
	local key="$1"

	printf '%s\n' "${2:-}" | tr '\r\n' '  ' | awk -v key="\"$key\"" '
		{
			pos = index($0, key)
			if (pos == 0)
				next
			value = substr($0, pos + length(key))
			if (value !~ /^[[:space:]]*:[[:space:]]*"/)
				next
			sub(/^[[:space:]]*:[[:space:]]*"/, "", value)
			end = index(value, "\"")
			if (end > 0) {
				print substr(value, 1, end - 1)
				exit
			}
		}
	'
}

runtime_slug="$(natter_runtime_slug "${NATTER_INSTANCE:-default}")"
status_file="${NATTER_STATUS_FILE:-/var/run/natter/${runtime_slug}.json}"
natter_write_status_json "$status_file" "${NATTER_INSTANCE:-default}" "$protocol" "$inner_ip" "$inner_port" "$outer_ip" "$outer_port" "mapped"

update_auto_firewall() {
	local enabled="${NATTER_AUTO_FIREWALL:-0}"
	local section="${NATTER_FIREWALL_SECTION:-natter_${runtime_slug}}"
	local name="${NATTER_FIREWALL_NAME:-Natter ${NATTER_INSTANCE:-default}}"
	local src="${NATTER_FIREWALL_SRC:-wan}"
	local dest="${NATTER_FIREWALL_DEST:-}"
	local port proto

	natter_bool "$enabled" || return 0
	case "$section" in
		''|*[!A-Za-z0-9_]*)
			section="natter_${runtime_slug}"
			;;
	esac
	port="$(natter_status_port "$inner_port")"
	[ "$port" != "0" ] || {
		log "auto firewall skipped for ${NATTER_INSTANCE:-default}: invalid port $inner_port"
		return 0
	}
	case "$protocol" in
		tcp|udp) proto="$protocol" ;;
		*) proto="tcp" ;;
	esac

	if ! command -v "$NATTER_UCI_BIN" >/dev/null 2>&1; then
		log "auto firewall skipped for ${NATTER_INSTANCE:-default}: uci not found"
		return 0
	fi

	"$NATTER_UCI_BIN" -q delete "firewall.${section}" >/dev/null 2>&1 || true
	if ! "$NATTER_UCI_BIN" set "firewall.${section}=rule" ||
		! "$NATTER_UCI_BIN" set "firewall.${section}.name=${name}" ||
		! "$NATTER_UCI_BIN" set "firewall.${section}.src=${src}" ||
		! "$NATTER_UCI_BIN" set "firewall.${section}.proto=${proto}" ||
		! "$NATTER_UCI_BIN" set "firewall.${section}.dest_port=${port}"; then
		log "auto firewall update failed for ${NATTER_INSTANCE:-default}"
		return 0
	fi
	if [ -n "$dest" ]; then
		if ! "$NATTER_UCI_BIN" set "firewall.${section}.dest=${dest}" ||
			! "$NATTER_UCI_BIN" set "firewall.${section}.dest_ip=${inner_ip}"; then
			log "auto firewall update failed for ${NATTER_INSTANCE:-default}"
			return 0
		fi
	fi
	if ! "$NATTER_UCI_BIN" set "firewall.${section}.target=ACCEPT" ||
		! "$NATTER_UCI_BIN" commit firewall; then
		log "auto firewall update failed for ${NATTER_INSTANCE:-default}"
		return 0
	fi

	if [ -x "$NATTER_FIREWALL_INIT" ]; then
		"$NATTER_FIREWALL_INIT" reload >/dev/null 2>&1 || log "firewall reload failed for ${NATTER_INSTANCE:-default}"
	fi
}

update_cloudflare_srv() {
	local enabled="${CLOUDFLARE_SRV_ENABLED:-0}"
	local url="${CLOUDFLARE_API_URL:-}"
	local token="${CLOUDFLARE_API_TOKEN:-}"
	local zone_id="${CLOUDFLARE_ZONE_ID:-}"
	local record_id="${CLOUDFLARE_RECORD_ID:-}"
	local base="${CLOUDFLARE_API_BASE:-https://api.cloudflare.com/client/v4}"
	local port timeout json response rc current_response current_port current_name current_target
	local patched_port patched_name patched_target

	natter_bool "$enabled" || return 0

	port="$(natter_status_port "$outer_port")"
	if [ "$port" = "0" ]; then
		log "Cloudflare SRV skipped for ${NATTER_INSTANCE:-default}: invalid outer port $outer_port"
		return 0
	fi

	if [ -z "$url" ] && [ -n "$zone_id" ] && [ -n "$record_id" ]; then
		url="${base%/}/zones/${zone_id}/dns_records/${record_id}"
	fi

	if [ -z "$url" ] || [ -z "$token" ]; then
		log "Cloudflare SRV is enabled but API URL/record or token is empty for ${NATTER_INSTANCE:-default}"
		return 0
	fi

	if ! command -v "$NATTER_CURL_BIN" >/dev/null 2>&1; then
		log "Cloudflare SRV skipped for ${NATTER_INSTANCE:-default}: curl not found"
		return 0
	fi

	timeout="${CLOUDFLARE_TIMEOUT:-8}"
	json='{"type":"SRV","data":{"port":'"$port"'}}'
	log "Cloudflare SRV update started for ${NATTER_INSTANCE:-default}: port=$port endpoint=$url"

	current_response="$("$NATTER_CURL_BIN" -fsS -m "$timeout" \
		-H "Authorization: Bearer $token" \
		-H "Accept: application/json" \
		"$url" 2>&1)"
	rc=$?
	if [ "$rc" -eq 0 ]; then
		current_port="$(natter_json_port "$current_response")"
		current_name="$(natter_json_string name "$current_response")"
		current_target="$(natter_json_string target "$current_response")"
		if [ -n "$current_port$current_name$current_target" ]; then
			log "Cloudflare SRV current record for ${NATTER_INSTANCE:-default}: name=${current_name:-unknown} target=${current_target:-unknown} port=${current_port:-unknown}"
		fi
	else
		log "Cloudflare SRV current record read failed for ${NATTER_INSTANCE:-default}: response=$(natter_one_line "$current_response")"
	fi

	response="$("$NATTER_CURL_BIN" -fsS -m "$timeout" -X PATCH \
		-H "Authorization: Bearer $token" \
		-H "Accept: application/json" \
		-H "Content-Type: application/json" \
		--data "$json" \
		"$url" 2>&1)"
	rc=$?
	if [ "$rc" -ne 0 ]; then
		log "Cloudflare SRV update failed for ${NATTER_INSTANCE:-default}: port=$port response=$(natter_one_line "$response")"
		return 0
	fi

	patched_port="$(natter_json_port "$response")"
	[ -n "$patched_port" ] || patched_port="$port"
	patched_name="$(natter_json_string name "$response")"
	patched_target="$(natter_json_string target "$response")"
	[ -n "$patched_name" ] || patched_name="${current_name:-}"
	[ -n "$patched_target" ] || patched_target="${current_target:-}"
	if [ -n "${current_port:-}" ]; then
		if [ "$current_port" = "$patched_port" ]; then
			log "Cloudflare SRV port unchanged for ${NATTER_INSTANCE:-default}: $patched_port name=${patched_name:-unknown} target=${patched_target:-unknown}"
		else
			log "Cloudflare SRV port changed for ${NATTER_INSTANCE:-default}: $current_port -> $patched_port name=${patched_name:-unknown} target=${patched_target:-unknown}"
		fi
	fi
	log "Cloudflare SRV updated for ${NATTER_INSTANCE:-default}: port=$patched_port name=${patched_name:-unknown} target=${patched_target:-unknown}"
}

update_auto_firewall
update_cloudflare_srv

run_user_notify() {
	local script="${NATTER_USER_NOTIFY:-}"

	[ -n "$script" ] || return 0
	if [ ! -x "$script" ]; then
		log "notify script is not executable for ${NATTER_INSTANCE:-default}: $script"
		return 1
	fi

	if ! "$script" "$protocol" "$inner_ip" "$inner_port" "$outer_ip" "$outer_port"; then
		log "notify script failed for ${NATTER_INSTANCE:-default}: $script"
		return 1
	fi

	return 0
}

enabled="${QBITTORRENT_ENABLED:-0}"
if [ "$enabled" != "1" ]; then
	run_user_notify
	exit 0
fi

url="$(natter_qb_normalize_url "${QBITTORRENT_URL:-}")"
username="${QBITTORRENT_USERNAME:-}"
password="${QBITTORRENT_PASSWORD:-}"
configured_port="${QBITTORRENT_TARGET_PORT:-0}"
listen_port="$(natter_qb_select_listen_port "$inner_port" "$outer_port" "$configured_port")"
if natter_qb_valid_port "$configured_port"; then
	listen_port_source="configured"
elif natter_qb_valid_port "$outer_port"; then
	listen_port_source="outer"
elif natter_qb_valid_port "$inner_port"; then
	listen_port_source="inner"
else
	listen_port_source="invalid"
fi
cookie="/tmp/natter-qbittorrent-${runtime_slug}.cookie"
json="$(natter_qb_preferences_json "$listen_port")"

cleanup_cookie() {
	rm -f "$cookie"
}
trap cleanup_cookie EXIT HUP INT TERM

if [ -z "$url" ] || [ -z "$username" ]; then
	log "qBittorrent is enabled but URL or username is empty for ${NATTER_INSTANCE:-default}"
	natter_write_status_json "$status_file" "${NATTER_INSTANCE:-default}" "$protocol" "$inner_ip" "$inner_port" "$outer_ip" "$outer_port" "qBittorrent URL or username is empty"
	run_user_notify
	exit 0
fi

if ! natter_qb_valid_port "$listen_port"; then
	log "qBittorrent listen_port is invalid for ${NATTER_INSTANCE:-default}: listen_port=$listen_port configured=$configured_port inner=$inner_port outer=$outer_port"
	natter_write_status_json "$status_file" "${NATTER_INSTANCE:-default}" "$protocol" "$inner_ip" "$inner_port" "$outer_ip" "$outer_port" "qBittorrent listen_port is invalid"
	run_user_notify
	exit 0
fi

rm -f "$cookie"
log "qBittorrent update started for ${NATTER_INSTANCE:-default}: url=$url listen_port=$listen_port source=$listen_port_source mapping=${protocol} ${inner_ip}:${inner_port} -> ${outer_ip}:${outer_port} forward=${QBITTORRENT_FORWARD:-0} target=${QBITTORRENT_TARGET_IP:-}:${QBITTORRENT_TARGET_PORT:-0} method=${QBITTORRENT_FORWARD_METHOD:-}"
login_response="$("$NATTER_CURL_BIN" -fsS -m "${QBITTORRENT_TIMEOUT:-8}" -c "$cookie" \
	--data-urlencode "username=$username" \
	--data-urlencode "password=$password" \
	"$url/api/v2/auth/login" 2>&1)"
login_rc=$?
if [ "$login_rc" -ne 0 ] || [ "$login_response" != "Ok." ]; then
	log "qBittorrent login failed for ${NATTER_INSTANCE:-default} at $url: response=$(natter_one_line "$login_response")"
	natter_write_status_json "$status_file" "${NATTER_INSTANCE:-default}" "$protocol" "$inner_ip" "$inner_port" "$outer_ip" "$outer_port" "qBittorrent login failed"
	rm -f "$cookie"
	run_user_notify
	exit 0
fi
log "qBittorrent login succeeded for ${NATTER_INSTANCE:-default} at $url"

preferences_response="$("$NATTER_CURL_BIN" -fsS -m "${QBITTORRENT_TIMEOUT:-8}" -b "$cookie" \
	"$url/api/v2/app/preferences" 2>&1)"
preferences_rc=$?
if [ "$preferences_rc" -eq 0 ]; then
	current_listen_port="$(natter_json_listen_port "$preferences_response")"
	[ -n "$current_listen_port" ] && log "qBittorrent current listen_port for ${NATTER_INSTANCE:-default}: $current_listen_port"
else
	current_listen_port=""
	log "qBittorrent current listen_port read failed for ${NATTER_INSTANCE:-default}: response=$(natter_one_line "$preferences_response")"
fi

set_response="$("$NATTER_CURL_BIN" -fsS -m "${QBITTORRENT_TIMEOUT:-8}" -b "$cookie" \
	--data-urlencode "json=$json" \
	"$url/api/v2/app/setPreferences" 2>&1)"
set_rc=$?
if [ "$set_rc" -ne 0 ]; then
	log "qBittorrent listen_port update failed for ${NATTER_INSTANCE:-default}: listen_port=$listen_port response=$(natter_one_line "$set_response")"
	natter_write_status_json "$status_file" "${NATTER_INSTANCE:-default}" "$protocol" "$inner_ip" "$inner_port" "$outer_ip" "$outer_port" "qBittorrent update failed"
	rm -f "$cookie"
	run_user_notify
	exit 0
fi

rm -f "$cookie"
if [ -n "${current_listen_port:-}" ]; then
	if [ "$current_listen_port" = "$listen_port" ]; then
		log "qBittorrent listen_port unchanged for ${NATTER_INSTANCE:-default}: $listen_port source=$listen_port_source mapping=${protocol} ${inner_ip}:${inner_port} -> ${outer_ip}:${outer_port}"
	else
		log "qBittorrent listen_port changed for ${NATTER_INSTANCE:-default}: $current_listen_port -> $listen_port source=$listen_port_source mapping=${protocol} ${inner_ip}:${inner_port} -> ${outer_ip}:${outer_port}"
	fi
fi
log "qBittorrent listen_port set to $listen_port for ${NATTER_INSTANCE:-default}; previous=${current_listen_port:-unknown} source=$listen_port_source mapping=${protocol} ${inner_ip}:${inner_port} -> ${outer_ip}:${outer_port}"
natter_write_status_json "$status_file" "${NATTER_INSTANCE:-default}" "$protocol" "$inner_ip" "$inner_port" "$outer_ip" "$outer_port" "qBittorrent listen_port set to $listen_port"
run_user_notify
exit 0
